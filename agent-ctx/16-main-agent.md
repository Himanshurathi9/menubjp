---
Task ID: 16
Agent: Main Agent
Task: Polish MenuMate owner dashboard pages for iOS-level aesthetic using design system

Work Log:
- Read worklog.md (15 previous tasks) and design-system.css for full context
- Read all 8 target files to understand current state
- Polished dashboard layout (layout.tsx): Changed `bg-white` to `var(--background)` CSS variable
- Polished dashboard home page (page.tsx):
  - Replaced inline hex colors with CSS variable `style` props throughout
  - Applied `.frosted-header`, `.card`, `.skeleton`, `.subscription-card` classes
  - Applied `.animate-page-enter` with staggered delays (1-5)
  - Applied `.animate-btn-press`, `.animate-pulse-dot` animation classes
  - Applied iOS toggle with `.toggle-track`/`.toggle-thumb` pattern
- Polished orders page (orders/page.tsx):
  - Applied `.frosted-header` to sticky header
  - Used design system CSS variables for all colors (text, status, badges)
  - Applied `.card`, `.badge`, `.btn` component classes
  - Applied `.skeleton`/`.skeleton-shimmer` for skeleton loaders
  - Applied `.animate-page-enter` with staggered delays for sections
  - Action buttons use `.btn` class with design system status colors (warning/success)
  - Order cards use `.card.rounded-card-lg` with proper hover transitions
  - Note section uses `var(--warning-soft)` background
  - Refresh button uses `var(--surface-raised)` background
- Polished menu page (menu/page.tsx):
  - Applied `.frosted-header` to sticky header
  - Applied `.card.rounded-card-lg` for item cards
  - Applied `.skeleton`/`.skeleton-shimmer` for skeleton loaders
  - Applied `.input` class for all form inputs
  - Applied `.btn`, `.btn-primary`, `.btn-accent`, `.btn-destructive`, `.btn-ghost` for buttons
  - Applied `.bottom-sheet`, `.bottom-sheet-backdrop`, `.bottom-sheet-drag-handle` for sheets
  - Applied `.toggle-track`/`.toggle-thumb` for iOS toggles
  - Category chips use design system variables
  - Food type colors use design system variables (success/error/warning)
  - Badge styling for Best Seller/Chef's Special
  - Floating add button uses `var(--shadow-fab)`
- Polished banners page (banners/page.tsx):
  - Added `.frosted-header` with proper title/subtitle layout
  - Applied `.card.rounded-card-lg` for banner cards
  - Applied `.skeleton`/`.skeleton-shimmer` for skeleton loader
  - Applied `.badge` for status badges with design system colors
  - Applied `.bottom-sheet`, `.bottom-sheet-backdrop`, `.bottom-sheet-drag-handle` for sheet
  - Applied `.input.rounded-full` for form inputs
  - Applied `.btn.btn-primary` for action buttons
  - Status badge colors: Active=success-soft/success, Scheduled=blue, Expired=error-soft/error, Inactive=surface-raised
  - Plan limit warning uses `var(--warning-soft)`
- Polished settings page (settings/page.tsx):
  - Added `.frosted-header` with proper layout
  - Applied `.card.rounded-card-lg` for all card sections
  - Applied `.skeleton`/`.skeleton-shimmer` for skeleton loader
  - Applied `.input.rounded-full` for all inputs
  - Applied `.btn`, `.btn-primary`, `.btn-secondary`, `.btn-accent` for all buttons
  - Loyalty section uses design system colors (warning-soft for PRO upgrade)
  - QR section uses accent-soft for section icon background
  - Regenerate confirmation uses error-soft for danger styling
  - Info box uses blue tones with proper opacity
  - Applied `.animate-page-enter` with staggered delays
- Polished BottomNav (BottomNav.tsx):
  - Replaced hex color values with CSS variables (`var(--text-primary)`, `var(--text-tertiary)`, `var(--accent)`, `var(--surface)`)
  - Already used `.frosted-bottom-nav` — maintained
- MenuItemCard and OrderCard remain as placeholders (no implementation needed)
- ESLint: zero errors
- Dev server: clean compilation, no warnings

Stage Summary:
- All 7 active files polished to consistently use the iOS design system
- All pages now use: frosted headers, CSS variable colors, component classes (.card, .btn, .input, .badge, .skeleton), animation classes (.animate-page-enter with stagger, .animate-btn-press, .animate-pulse-dot, .animate-fade-in, .animate-tab-spring)
- All interactive elements maintain min-h-[44px] tap targets
- All buttons are pill-shaped (rounded-full / btn class)
- Skeleton loaders use shimmer (not spinners)
- No dark mode — white/light theme only
- Zero functionality changes — only visual presentation updated
- ESLint zero errors, dev server compiles cleanly (~150-250ms)
